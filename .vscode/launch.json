{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run dev",
            "type": "node-terminal",
            "request": "launch",
            "command": "pnpm run dev --host",
            "cwd": "${workspaceFolder}"
        }
    ]
}